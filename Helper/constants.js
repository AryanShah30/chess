// used to store constants, making it easier for reference and consistency

// retrieves the HTML element with id 'root'
// ROOT_DIV serves as a container for rendering dynamic content
const ROOT_DIV = document.getElementById("root");

export { ROOT_DIV };
